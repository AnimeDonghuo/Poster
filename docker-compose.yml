version: "3.9"
services:
  poster-bot:
    build: .
    container_name: poster-bot
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - TMDB_API_KEY=${TMDB_API_KEY}
      - OMDB_API_KEY=${OMDB_API_KEY}
      - DEBUG=${DEBUG:-false}
    restart: unless-stopped
